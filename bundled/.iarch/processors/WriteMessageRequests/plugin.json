{
  "version": "1.0.0",
  "processor-path": "./write_message_requests.py",
  "processors": [
    {
      "id": "write-message-requests",
      "name": "Write Message Requests",
      "requires": ["violations"],
      "optional-requires": ["ci-environment", "created-tickets"],
      "provides": ["message-requests"],
      "ide-hidden": true
    }
  ]
}
