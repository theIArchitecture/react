{
  "name": "heal-configure-await",
  "description": "Heal ConfigureAwait violations: full C# scan, then Roslyn-based configure-await healing, annotate and flush with backup.",
  "processors": [
    "@core",
    "@intake/directory",
    "@data/scan",
    "@parse/graph",
    "@validate/files",
    "@validate/tree",
    "@violations/finalize",
    "configure-await-validator",
    "configure-await-healer",
    "annotation-flush"
  ],
  "config": {
    "annotation-flush.backup": "true"
  }
}
