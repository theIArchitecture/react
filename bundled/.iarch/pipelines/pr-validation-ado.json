{
  "name": "pr-validation-ado",
  "description": "PR validation (Azure DevOps): scan, heal, annotate, report to ADO, record history. Identical to pr-validation — only @pr/ado + @publish/ado differ.",
  "processors": [
    "@scan/pr",
    "@heal/all",
    "@annotate",
    "@pr/ado",
    "@publish/ado"
  ],
  "config": {
    "bot-target-path": "project-root",
    "bot-commit-message": "chore: update pr-scan-history [skip ci]",
    "commit-caches": "pr-scan-history.json,pipeline-run-history"
  }
}
