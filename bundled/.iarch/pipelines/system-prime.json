{
  "name": "system-prime",
  "description": "Cold start: build and cache the dependency graph and violation index. No validators — just primes the caches so subsequent IDE runs are fast.",
  "processors": [
    "@core",
    "@intake/directory",
    "@data/scan",
    "@parse/graph",
    "@violations/finalize"
  ],
  "config": {
    "ide-hidden": "true"
  }
}
