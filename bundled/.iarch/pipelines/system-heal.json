{
  "name": "system-heal",
  "description": "LS single-file heal: load cached state, validate, heal with pattern and AI, annotate.",
  "processors": [
    "@core",
    "@intake/pr-diff",
    "@data/scan-readonly",
    "@validate/files",
    "@heal/all",
    "@annotate"
  ],
  "config": {
    "ide-hidden": "true"
  }
}
