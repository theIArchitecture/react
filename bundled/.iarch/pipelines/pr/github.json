{
  "name": "PR — GitHub reporting",
  "description": "Full GitHub PR feedback bundle: CI environment + check run + PR comment + commit markers + PR scan history. Swap for @pr/ado to move to Azure DevOps.",
  "processors": [
    "@platform/github",
    "@platform/github-pr",
    "@history/pr-scan"
  ],
  "config": {}
}
