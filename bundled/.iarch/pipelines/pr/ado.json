{
  "name": "PR — ADO reporting",
  "description": "Full ADO PR feedback bundle: CI environment + status check + PR comment + commit markers + PR scan history. Swap for @pr/github to move to GitHub.",
  "processors": [
    "@platform/ado",
    "@platform/ado-pr",
    "@history/pr-scan"
  ],
  "config": {}
}
